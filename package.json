{
  "name": "openemployee",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turn OpenClaw into a governed AI employee: one role, deny-by-default skill grants, segregation of duties, approval gates, and a tamper-evident audit trail — via MakerChecker.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" build",
    "typecheck": "pnpm -r --filter \"./packages/**\" typecheck",
    "test": "pnpm -r --filter \"./packages/**\" test",
    "lint": "pnpm -r --filter \"./packages/**\" lint"
  }
}
